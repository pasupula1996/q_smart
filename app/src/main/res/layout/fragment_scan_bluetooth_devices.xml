<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:padding="10dp">

    <TextView
        android:paddingTop="10dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Available Devices"
        android:textSize="18sp"
        android:id="@+id/available_bt"/>
    <ProgressBar
        android:id="@+id/scan_bt_progress"
        android:layout_marginTop="10dp"
        android:layout_width="25dp"
        android:layout_height="30dp"
        android:paddingLeft="10dp"
        android:layout_toRightOf="@id/available_bt"
        android:layout_toEndOf="@id/available_bt"
        android:layout_marginEnd="10dp"
        android:layout_marginRight="10dp"
        android:visibility="gone"/>
    <ListView
        android:layout_below="@+id/available_bt"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/lv_scan_bluetooth"
        android:layout_centerHorizontal="true"
        android:paddingTop="10dp"/>

</RelativeLayout>